<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.stopwatch.StopWatchActivity">

    <TextView
        android:id="@+id/stopwatch_activity_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.367"
        android:textColor="@color/TextColor"
        android:text="@string/start_time"
        android:textSize="40sp"
        android:fontFamily="@font/inter"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/stopwatch_activity_start_btn"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="50dp"
        android:backgroundTint="?android:attr/windowBackground"
        android:src="@drawable/ic_start"
        app:layout_constraintEnd_toEndOf="@+id/stopwatch_activity_countdown"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="@+id/stopwatch_activity_countdown"
        app:layout_constraintTop_toBottomOf="@+id/stopwatch_activity_countdown"
        tools:ignore="SpeakableTextPresentCheck" />

    <ImageButton
        android:id="@+id/stopwatch_activity_stop_btn"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="25dp"
        android:backgroundTint="?android:attr/windowBackground"
        android:src="@drawable/ic_stop"
        app:layout_constraintEnd_toEndOf="@+id/stopwatch_activity_start_btn"
        app:layout_constraintStart_toStartOf="@+id/stopwatch_activity_start_btn"
        app:layout_constraintTop_toBottomOf="@+id/stopwatch_activity_start_btn"
        tools:ignore="SpeakableTextPresentCheck" />

    <ImageButton
        android:id="@+id/stopwatch_activity_reset_btn"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="25dp"
        android:backgroundTint="?android:attr/windowBackground"
        android:src="@drawable/ic_reset"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@+id/stopwatch_activity_start_btn"
        app:layout_constraintStart_toStartOf="@+id/stopwatch_activity_start_btn"
        app:layout_constraintTop_toBottomOf="@+id/stopwatch_activity_start_btn"

        tools:ignore="SpeakableTextPresentCheck" />

</androidx.constraintlayout.widget.ConstraintLayout>